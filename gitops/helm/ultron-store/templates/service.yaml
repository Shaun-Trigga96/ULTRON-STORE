apiVersion: v1
kind: Service
metadata:
  name: {{ include "ultron-store.fullname" . }}
  labels:
    {{- include "ultron-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ultron-store.selectorLabels" . | nindent 4 }}
